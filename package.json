{
  "name": "xsnvshen-paperback",
  "version": "1.0.0",
  "description": "Paperback 0.8 source for xsnvshen.com",
  "repositoryName": "XiuShen Source",
  "scripts": {
    "test:parser": "npx ts-node scripts/test-parser.ts",
    "build": "npx paperback bundle",
    "serve": "npx paperback serve",
    "test": "npx paperback test"
  },
  "dependencies": {
    "@paperback/toolchain": "0.8.0-alpha.47",
    "@paperback/types": "0.8.0-alpha.47"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^4.3.2"
  }
}
